#include <iostream>
#include <cmath> // includes: pow, sqrt

int main() {
    double x = 3.84;
    double y = 4.14;
    double a = 7;
    double z;

    // Built-ins: max, min, abs

    //z = std::max(x, y);
    //z = std::min(x, y);
    // z = pow(2, 4);
    // z = sqrt(9);
    //z = std::abs(-3);
    //z = round(x);
    //z = ceil(x);
    z = floor(x);


    std::cout << z << std::endl;
}